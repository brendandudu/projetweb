<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Info</title>
    <script src="./js/vue.js"></script>
    <!-- 引入样式 -->
    <link rel="stylesheet" href="https://unpkg.com/element-ui/lib/theme-chalk/index.css">
    <!-- 引入组件库 -->
    <script src="https://unpkg.com/element-ui/lib/index.js"></script>
    <link href="{{ asset('css/user.css') }}" rel="stylesheet"/>
    <link href="{{ asset('css/lodgingdetails.css') }}" rel="stylesheet"/>
</head>
<body>
<div id="app">
    {% include 'includes/header.html.twig' %}
    <div class="bottom">
        <div class="address">{{ booking.beginsAt|date("m-d-Y") }} - {{ booking.endsAt|date("m-d-Y") }}</div>
        <div>
            <img  class="myreservationpic" src="{{ lodging.picture}}" alt="Generic placeholder image">
        </div>
        <div >

            <div class="welcome">Votre séjour chez {{lodging.name}}</div>

            <div class="twoboxes">
                <div class="departdiv1">
                    Arrivé
                    <div class="date">{{ booking.beginsAt|date("m-d-Y") }}</div>
                </div>
                <div class="departdiv2">
                    Départ
                    <div class="date">{{ booking.endsAt|date("m-d-Y") }}</div>
                </div>
            </div>
            <div class="infodiv">
                <div class="info">Aperçu</div>
                <div class="detailinfo">
                    Hote: {{lodging.name}} <br/>
                    Logement entire: {{ booking.totalOccupiers}} <br/>
                    Address: {{ lodging.fullAddress}}<br/>
                    €{{booking.totalPricing}}
{#                    <div>#}
{#                        <div class="price">{{lodging.nightPrice}} €<small>{{lodging.lodgingType}}</small></div>#}
{#                        <div class="stats">#}
{#                            <span><i class="fa fa-arrows-alt"></i>{{ lodging.space}} m2</span>#}
{#                            <span><i class="fa fa-user"></i>{{ lodging.capacity }} </span>#}
{#                        </div>#}
{#                    </div>#}
                    <div class="des">
                        {{lodging.description}}
                    </div>
                </div>
            </div>
            <div class="box3">
                <span class="details">Détails de la réservation  </span>


                <div class="boldtext">Condition d'annulation</div>
                <div class="condition">Si vues vulez votre réservation, contectez l'hote au moins 48 heures avant votre arrivée </div>

                <div class="boldtext" >Code de confirmation</div>
                <div class="code">{{ booking.id }}</div>


            </div>
            <div class="bottomdiv">
                <img class="logos" src='{{ asset('assets/img/logo1.png') }}' ><span class="logoinline" >Obtenir un PDF pour la visa</span><span class="arrow">> </span>
            </div>
            <div class="bottomdiv"><span class="arrow">> </span>
                <img class="logos" src='{{ asset('assets/img/logo2.png') }}' ><span class="logoinline" >Imprimer le informations</span>
            </div>
            <div class="bottomdiv">
                <img class="logos" src='{{ asset('assets/img/logo3.png') }}' ><span class="logoinline" >Obtenir le recu</span><span class="arrow">> </span>
            </div>


        </div>
    </div>
</div>

<!-- import Vue before Element -->
<script src="https://unpkg.com/vue/dist/vue.js"></script>
<!-- import JavaScript -->
<script src="https://unpkg.com/element-ui/lib/index.js"></script>
<script>
    new Vue({
        el: '#app',
        data: function() {
            return { visible: false }
        }
    });
    $(function(){
        $('.listing-block').slimScroll({
            height: '500px'
        });
    });
</script>
</body>
</html>


