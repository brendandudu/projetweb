{{ form_start(formBooking) }}
<div class="row justify-content-center text-center">
    <div class="col-md-3">
        {{ form_widget(formBooking.beginsAt) }}
    </div>
    <div class="col-md-2">
        <i class="fas fa-arrow-right arrow"></i>
    </div>
    <div class="col-md-3">
        {{ form_widget(formBooking.endsAt) }}
    </div>
</div>
<div class="row justify-content-end mt-5">
    <div class="col-md-2 text-right pr-0">
        {{ form_widget(formBooking.totalOccupiers, {'attr': { 'class' : 'input-occupiers'}}) }} <i class="fa fa-user"></i>
    </div>
    <div class="col-md-1">
        <button type='submit' class="btn-submit">Réserver</button>
    </div>
</div>
{{ form_end(formBooking) }}